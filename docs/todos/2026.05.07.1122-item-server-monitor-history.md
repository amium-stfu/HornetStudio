# TODO

## Title
Add history support to Item Server Monitor.

## Problem
The first `Item.Server.Monitor` implementation shows only the latest snapshot and intentionally omits historical capture or trend views.

## Impact
Operators can inspect current runtime state, but cannot review short-term value evolution or compare recent changes while monitoring live items.

## Suggested Fix
Add an opt-in bounded history buffer per monitored path and a dedicated detail visualization that stays separate from the flat live tree hot path.

## Priority
Medium

## Related Files
- src/Item.Server.Monitor/ViewModels/MainWindowViewModel.cs
- src/Item.Server.Monitor/Monitoring/MonitorSnapshotStore.cs

## Notes
- Keep history capture optional and throttle-aware.
- Do not compromise visible-row update performance.
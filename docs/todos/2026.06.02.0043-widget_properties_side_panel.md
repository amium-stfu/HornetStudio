# TODO

## Title
Add a tabbed right-side properties panel for widget configuration.

## Problem
Widget settings currently open in separate windows, which can briefly block the shared Avalonia UI thread and interrupt the main dashboard interaction.

## Impact
A docked context panel would keep the dashboard visible while editing widget settings and reduce window-management friction during frequent widget configuration.

## Suggested Fix
Convert the existing right-side folder legend area into a Visual Studio-style context panel with tabs such as `Folders` and `Properties`. Keep `Folders` as the current navigation/legend view and use `Properties` for the selected widget. Activate the `Properties` tab from widget selection or settings actions, and keep expensive settings initialization lazy or asynchronous.

## Priority
Medium

## Related Files
- src/HornetStudio
- src/HornetStudio.UiEditor

## Notes
- Preserve the current folder legend behavior as one panel state.
- Avoid overlaying the chart/dashboard content with settings controls.
- The side panel improves UX, but UI-thread freezes still need profiling if settings initialization remains expensive.

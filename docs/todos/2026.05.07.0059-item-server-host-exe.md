# TODO

## Title
Add a startable Item Server host executable.

## Problem
After merging the former service startup into `Amium.Item.Server`, the server runtime is available as a library but no dedicated executable starts the default MQTT item server host.

## Impact
Users cannot start the Item Server as a standalone process without creating a separate launcher or calling `ItemServerServiceHost.RunAsync` from another application.

## Suggested Fix
Create a small dedicated console host project, for example `Amium.Item.Server.Host`, that references `Amium.Item.Server` and runs `ItemServerServiceHost.RunAsync`.

## Priority
Medium

## Related Files
- src/Amium.Item.Server/ItemServerServiceHost.cs
- src/Amium.Item.Server/Amium.Item.Server.csproj
- HornetStudio.sln

## Notes
- Keep the server runtime project as a library so client and integration projects can reference it cleanly.
- Do not reintroduce server behavior into the client packages.

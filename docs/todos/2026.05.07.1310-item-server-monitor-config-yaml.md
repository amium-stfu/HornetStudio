# TODO

## Title
Support monitor adapter startup from config.yaml.

## Problem
`Item.Server.Monitor` currently exposes adapter options only through the UI and does not restore or auto-start adapters from configuration.

## Impact
The monitor cannot yet reopen with predefined adapter settings or controlled startup behavior.

## Suggested Fix
Load monitor adapter definitions from `config.yaml`, map them onto `MonitorAdapterOptions`, and optionally auto-start selected adapters after the local runtime is initialized.

## Priority
Medium

## Related Files
- src/Item.Server.Monitor/Hosting/MonitorHost.cs
- src/Item.Server.Monitor/ViewModels/MonitorAdapterViewModel.cs

## Notes
- Reuse the current UI option model instead of introducing a second config-only shape.
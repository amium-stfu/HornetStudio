# IMPLEMENTATION HANDOFF

## Goal
Fix the remaining functional monitor regressions before continuing benchmark tuning:

1. Monitor actions are configured in `Monitoring/Monitor.yaml` but are not executed correctly.
2. The Monitor Browser ComboBox/Edit path no longer opens the monitor edit dialog.
3. After both functional issues are fixed, continue the MonitorRuntime policy cleanup:
   - folder-local `runtime.startupDelayMs`
   - rule root publish only initially and on bool changes
   - actions only on `false -> true`

## Scope

Included:

- Diagnose and fix monitor action execution.
- Diagnose and fix Monitor Browser edit-dialog access from the ComboBox/Edit UI.
- Keep AlertWidget/MonitorView root-only behavior intact.
- Continue runtime policy cleanup only after functional behavior works.
- Add/update focused tests for action target resolution and browser edit command behavior where feasible.
- Run focused validation before any benchmark.

Not included:

- No new Monitor UI features.
- No Monitor Browser redesign beyond restoring edit behavior.
- No per-rule startup delay.
- No project-level runtime settings in `project.aaep`.
- No new dependencies.
- No release/version work.

## Starting Point

- Existing workitem:
  - `docs/workitems/2026.06.02.1209-monitor_widget_explicit_rules/`
- Previous runtime handoff:
  - `docs/workitems/2026.06.02.1209-monitor_widget_explicit_rules/handoffs/2026.06.03.0844-implementation-handoff.md`
- Benchmark project:
  - `benchmarks/ui_lag_bench_project/project.aaep`
- Monitor file:
  - `benchmarks/ui_lag_bench_project/Folders/main/Monitoring/Monitor.yaml`
- Current monitor action examples:

```yaml
actions:
- actionType: SetValue
  targetLog: ''
  targetPath: enhanced_signals.filtered_1.set
  functionName: ''
  argument: 20
```

- Recent 60s benchmark:
  - Scenario: `MonitorRuntimePublishOnChange-60s`
  - Status: `Warn`
  - `BrowserDispatcherPosts=33`
  - Child `.active`/`.message` events are no longer the main issue.
  - Root events remain: `11` per rule in 60s.
  - Warnings repeatedly show:

```text
[Monitor] SetValue target 'studio.enhanced_signals.filtered_1.set' for rule 'monitor_rule_*' could not be resolved.
```

- User reports:
  - Actions are not executed although configured.
  - In Monitor Browser, ComboBox/Edit no longer opens the edit dialog.

## Tasks

1. Fix monitor action execution first.
   - Inspect `MonitorRuleRuntime` action dispatch.
   - Inspect `SetValue` target resolution for monitor actions.
   - Determine whether `targetPath: enhanced_signals.filtered_1.set` should resolve folder-relative.
   - The warning currently suggests it becomes `studio.enhanced_signals.filtered_1.set`, likely missing the folder segment.
   - Expected folder-scoped resolution is likely `studio.main.enhanced_signals.filtered_1.set` or the existing canonical path used by Signal/EnhancedSignals.
   - Fix target path resolution using existing project/folder path helper patterns.
   - Ensure configured actions execute when the monitor becomes active.

2. Fix Monitor Browser edit dialog access.
   - Inspect Monitor Browser ComboBox/Edit UI and command/event handling.
   - Restore the expected flow from selecting/editing a monitor rule to opening `MonitorEditorDialogWindow`.
   - Verify the dialog receives the correct folder context and selected `MonitorDefinition`.
   - Ensure saving from the dialog still writes the central `Monitoring/Monitor.yaml` file, not stale embedded widget definitions.

3. Add focused functional tests.
   - Action target path resolution:
     - relative target path resolves in the owning folder scope.
     - configured `SetValue` action updates the intended target when a rule becomes active.
   - Action failure logging:
     - unresolved target still logs a useful warning with rule name and resolved path.
   - Browser edit:
     - test command/view-model path if an existing non-UI test seam exists.
     - If no test seam exists, document manual verification explicitly.

4. Only then continue runtime policy cleanup.
   - Keep the contract from the previous handoff:
     - `Monitoring/Monitor.yaml` supports `runtime.startupDelayMs`.
     - Rule root bool is published once initially and then only on bool changes.
     - Actions execute only on `false -> true`.
     - No regular `.active`/`.message` child publishes for SignalWidget/AlertWidget.
   - Do not hide action failures by reducing execution frequency before path resolution is fixed.

5. Validate before benchmark.
   - Run focused tests for Monitor runtime/action behavior.
   - Run focused tests for Monitor Browser if available.
   - Manually verify browser edit dialog if no automated coverage exists.
   - Only then run `#bench 60` or the benchmark command listed below.

## File-Level Changes

- `src/HornetStudio.Editor/Monitoring/MonitorRuleRuntime.cs`
  - Primary target for action execution, action trigger policy, and warning behavior.

- `src/HornetStudio.Editor/Monitoring/MonitorRuntimeManager.cs`
  - Continue folder-level runtime settings/startup delay work if not completed.

- `src/HornetStudio.Editor/Monitoring/MonitorDefinitionFileCodec.cs`
  - Continue `runtime.startupDelayMs` document support if not completed.

- `src/HornetStudio.Editor/Models/MonitorDefinition.cs`
  - Runtime settings model location if already chosen there.

- `src/HornetStudio.Editor/Widgets/Monitor/MonitorBrowserDialogWindow.axaml`
- `src/HornetStudio.Editor/Widgets/Monitor/MonitorBrowserDialogWindow.axaml.cs`
  - Restore ComboBox/Edit dialog flow.

- `src/HornetStudio.Editor/Widgets/Monitor/MonitorEditorDialogWindow.axaml.cs`
  - Inspect/fix only if dialog invocation or persistence context is wrong.

- `src/HornetStudio.Editor/Widgets/Monitor/MonitorControl.axaml.cs`
  - Inspect central `Monitoring/Monitor.yaml` load/save and runtime sync wiring.

- `src/HornetStudio.Editor/ViewModels/MainWindowViewModel.cs`
  - Inspect only if monitor runtime sync or browser launch is wired here.

- `src/HornetStudio.Editor.Tests/Program.cs`
  - Add/update focused monitor action and codec/runtime tests.

- `benchmarks/ui_lag_bench_project/Folders/main/Monitoring/Monitor.yaml`
  - Keep rules unchanged unless needed for benchmark validation.
  - Add/keep `runtime.startupDelayMs` only as part of runtime setting validation.

## Implementation Order

1. Read current action execution and target resolution in `MonitorRuleRuntime`.
2. Fix folder-relative monitor action target resolution.
3. Add a focused test proving `enhanced_signals.filtered_1.set` resolves/executed in folder scope.
4. Read Monitor Browser edit UI and event/command path.
5. Fix ComboBox/Edit opening `MonitorEditorDialogWindow`.
6. Add test coverage for edit command if existing patterns allow it; otherwise record manual verification.
7. Re-run focused functional tests.
8. Continue publish-on-change/action-on-edge/startup-delay cleanup from the previous handoff.
9. Run focused runtime tests.
10. Run benchmark only after functional tests and manual edit verification pass.

## Technical Constraints

- Chat response must be German; code, comments, XML documentation, UI text, validation messages, and technical identifiers must be English.
- Use existing path resolution helpers and registry patterns.
- No new dependencies.
- Runtime services remain owned by runtime managers, not visual controls.
- Do not add per-rule delay.
- Do not add UI for `runtime.startupDelayMs` in this step.
- Do not mask unresolved action targets; keep explicit warnings.

## Acceptance Criteria

- Configured monitor `SetValue` actions execute successfully for folder-relative targets.
- The warning `SetValue target 'studio.enhanced_signals.filtered_1.set' ... could not be resolved` no longer appears for the benchmark project's valid target.
- Monitor Browser ComboBox/Edit opens the monitor edit dialog again.
- Editing a rule uses the correct selected rule and folder context.
- Saving edits persists to `Monitoring/Monitor.yaml`.
- Runtime policy remains or becomes:
  - initial root publish once
  - subsequent root publish only on bool change
  - actions only on `false -> true`
  - no regular `.active`/`.message` child publishes for the normal widget path
- AlertWidget/MonitorView still observe rule-root bool semantics.

## Verification

Focused build:

```powershell
dotnet build src\HornetStudio.Editor.Tests\HornetStudio.Editor.Tests.csproj --no-restore -p:OutDir=B:\HornetStudio\artifacts\editor-tests\
```

Focused tests:

```powershell
$env:HORNET_EDITOR_TEST_FILTER='Monitor runtime'; dotnet B:\HornetStudio\artifacts\editor-tests\HornetStudio.Editor.Tests.dll
$env:HORNET_EDITOR_TEST_FILTER='Monitor file codec'; dotnet B:\HornetStudio\artifacts\editor-tests\HornetStudio.Editor.Tests.dll
$env:HORNET_EDITOR_TEST_FILTER='Monitor browser'; dotnet B:\HornetStudio\artifacts\editor-tests\HornetStudio.Editor.Tests.dll
$env:HORNET_EDITOR_TEST_FILTER='Alert widget row'; dotnet B:\HornetStudio\artifacts\editor-tests\HornetStudio.Editor.Tests.dll
```

Manual verification:

- Start the app with `benchmarks/ui_lag_bench_project/project.aaep`.
- Open Monitor Browser.
- Use the ComboBox/Edit path to open the edit dialog for an existing rule.
- Save a harmless edit or cancel after confirming the dialog opens with the correct rule.
- Confirm monitor actions execute against the intended folder-scoped target.

Benchmark after functional fixes:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run_ui_benchmark.ps1 -Seconds 60 -ProjectPath benchmarks\ui_lag_bench_project\project.aaep -Scenario MonitorRuntimeFunctionalFixes-60s
```

## Out of Scope

- No Monitor Browser redesign.
- No global project runtime settings.
- No per-rule startup delay.
- No release/version work.
- No broad widget refactor.

## Risks / Watchouts

- Do not reduce action execution frequency before fixing target resolution; otherwise the functional action bug may be hidden.
- If the target path is intentionally not folder-relative, document the actual path contract and update the benchmark project accordingly.
- If Browser edit broke because definitions moved to central `Monitor.yaml`, make sure the edit flow uses the central file source and not stale embedded `MonitorDefinitions`.
- Manual UI verification may be required if the browser edit flow has no existing test seam.

## Relevant Files

- `src/HornetStudio.Editor/Monitoring/MonitorRuleRuntime.cs`
- `src/HornetStudio.Editor/Monitoring/MonitorRuntimeManager.cs`
- `src/HornetStudio.Editor/Monitoring/MonitorDefinitionFileCodec.cs`
- `src/HornetStudio.Editor/Models/MonitorDefinition.cs`
- `src/HornetStudio.Editor/Widgets/Monitor/MonitorBrowserDialogWindow.axaml`
- `src/HornetStudio.Editor/Widgets/Monitor/MonitorBrowserDialogWindow.axaml.cs`
- `src/HornetStudio.Editor/Widgets/Monitor/MonitorEditorDialogWindow.axaml.cs`
- `src/HornetStudio.Editor/Widgets/Monitor/MonitorControl.axaml.cs`
- `src/HornetStudio.Editor/ViewModels/MainWindowViewModel.cs`
- `src/HornetStudio.Editor.Tests/Program.cs`
- `benchmarks/ui_lag_bench_project/Folders/main/Monitoring/Monitor.yaml`
- `benchmarks/ui_lag_bench_project/project.aaep`

## Notes

- Start the new chat from this handoff, not from benchmark tuning alone.
- Functional correctness comes first: actions and edit dialog must work before interpreting benchmark results.
- The previous benchmark proved that UI child-event filtering is no longer the primary problem.

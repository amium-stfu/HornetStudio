# DEBUG REPORT

## Problem
Functions Edit Step `SetValue` rows do not visually match the Interaction Rules `SetValue` rows.

## Expected Behavior
- No SetValue summary is shown in Functions Edit Step rows.
- The target, operation, literal/source editor, and action buttons align like the Interaction Rules editor.
- Source selection appears directly in the argument column after the operation picker.

## Actual Behavior
- A SetValue summary is rendered even though it is redundant in the Functions row.
- The summary is placed in the same grid column as the source picker, causing visible overlap.
- The source picker is pushed to the right because hidden literal and separator columns still reserve layout space.

## Error Messages / Logs
No runtime error. The defect is visual layout behavior visible in the provided screenshot comparison.

## Relevant Code
- `src/HornetStudio.Editor/Widgets/Workflow/WorkflowEditorDialogWindow.axaml`
- `src/HornetStudio.Editor/Widgets/Common/InteractionRulesEditorDialogWindow.axaml`

## Attempted Fixes
- Fix 1 -> Reworked the Functions SetValue row grid to use a single argument column with nested literal/source editors, removed the summary block, and moved validation text to a second row in the argument column. Result: `dotnet build src\HornetStudio.Editor\HornetStudio.Editor.csproj --no-restore` passed with 0 warnings and 0 errors.

## Validation
- `dotnet build HornetStudio.sln --no-restore` reached project compilation but failed because the running `HornetStudio` process and Visual Studio locked files in `src\HornetStudio\bin\Debug\net9.0-windows`.
- `dotnet build src\HornetStudio.Editor\HornetStudio.Editor.csproj --no-restore` passed.

## Current Hypothesis
- The XAML layout now matches the Interaction Rules structure closely enough: target + browse, operation picker, argument/source editor, then row action buttons.

# UDL Sinus Gap Baseline Findings

## Baseline Run

- Scenario: `UdlSinusGapBaseline-120s`
- Project: `benchmarks/ui_lag_bench_project/project.aaep`
- Date: 2026-06-03
- Result: `UI benchmark summary` logged `Status="Pass"` with `BrowserDispatcherPosts=0` and `ChartRenderCount=0`.

## Target Path

- The current minimal benchmark signal is `udl1.m002.read`.
- The benchmark project does not currently exercise `udl1.m001` in the placed Signal widget.

## Gap Diagnostics

- `DemoGenerate|udl1.m002.read` stayed near `108-110 ms`.
- `RegistryPublish|udl1.m002.read` stayed near `108-110 ms`.
- `SignalHistoryAppend|udl1.m002.read` stayed near `164-182 ms`.
- No stage reported `GapsOver250Ms > 0` or `GapsOver1000Ms > 0` during the 120 s baseline.

## Interpretation

- The demo generator and the public registry publish path remained stable during the baseline run.
- The only visible cadence difference is the slower SignalHistory sampling path, but it remained stable and did not show long gaps.
- Based on this baseline, the current evidence points away from the UDL source/publish path as the origin of long stalls.

## Follow-up Limitation

- The requested `SignalWidget settings opened once` benchmark was not automated in this turn.
- `scripts/run_ui_benchmark.ps1` only starts and times the application; it does not contain a settings-open interaction step.
- No repository-local desktop UI automation path for that interaction was found during this implementation pass.
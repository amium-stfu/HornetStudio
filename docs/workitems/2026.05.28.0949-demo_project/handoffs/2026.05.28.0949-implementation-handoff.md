# IMPLEMENTATION HANDOFF

## Goal
Create a portable, versioned HornetStudio demo project inside the repository that can be used as the shared manual QA and future migration reference project for folder-level resources such as Functions and later `Signals/Enhanced/*.yaml`.

## Scope
Included:
- Add a repository-local demo project under `samples/HornetStudioDemo/`.
- Use `T:/HornetStudio/NewFirst` only as a source reference, not as a persisted path.
- Copy or recreate the useful project layout and folder UI data in a portable form.
- Exclude generated runtime logs and machine-local artifacts.
- Sanitize any password or user credential content before committing.
- Add a short README explaining how to open and use the demo project.
- Ensure the demo project is not added to `HornetStudio.sln` as a build project.
- Prepare the sample folder structure for future resource extraction, especially `Signals/Enhanced/*.yaml`.

Not included:
- Implementing the EnhancedSignals extraction itself.
- Migrating current EnhancedSignals data out of `Folder.yaml` unless a tiny placeholder folder is needed to document the intended target structure.
- Adding runtime dependencies.
- Release/version changes.
- Changing application loading behavior unless the existing loader cannot open a relative sample project.

## Starting Point
- External reference project exists at `T:/HornetStudio/NewFirst`.
- The external reference has:
  - `project.aaep`
  - `Folders/main/Folder.yaml`
  - `Folders/main/Applications/`
  - `Folders/main/Assets/`
  - `Folders/main/Scripts/Workflows/`
  - `Logs/` with generated runtime log files
- `project.aaep` currently contains simple password values and a save timestamp; do not blindly copy credentials.
- The repository currently has no top-level `samples/` folder.
- The intended future resource layout is:
  - `Folders/<folder>/Signals/Enhanced/*.yaml`
  - later `Folders/<folder>/Signals/Custom/*.yaml`
  - existing/future function files under `Folders/<folder>/Scripts/Functions/*.yaml`

## Tasks
1. Create `samples/HornetStudioDemo/` as the canonical repository demo project location.
2. Copy or reconstruct the reference project structure from `T:/HornetStudio/NewFirst` into the sample folder while keeping paths relative and portable.
3. Include `project.aaep` only after sanitizing credentials and machine-specific or volatile metadata.
4. Include `Folders/main/Folder.yaml` after checking for absolute local paths, machine-specific values, secrets, and excessive generated data.
5. Copy useful folder assets/applications/scripts only if they are portable and relevant to the demo.
6. Do not copy `Logs/` runtime output into the demo project.
7. Add empty placeholder directories only where useful and trackable, using `.gitkeep` if needed:
   - `samples/HornetStudioDemo/Folders/main/Signals/Enhanced/`
   - optionally `samples/HornetStudioDemo/Folders/main/Signals/Custom/`
8. If legacy workflow files exist below `Scripts/Workflows/`, decide whether to keep them as legacy reference or move/copy them to `Scripts/Functions/` only if current app behavior expects that for the demo.
9. Add `samples/HornetStudioDemo/README.md` with:
   - purpose
   - how to open `project.aaep`
   - what the demo contains
   - what must not be committed, especially generated logs and local secrets
10. Update repository documentation only if there is an existing sample/demo index; otherwise keep documentation local to the sample README.
11. Add or update `.gitignore` only if generated demo runtime output would otherwise be tracked.

## File-Level Changes
- `samples/HornetStudioDemo/project.aaep` -> Portable sanitized demo project entry file.
- `samples/HornetStudioDemo/Folders/main/Folder.yaml` -> Demo folder UI layout copied or reconstructed from the external reference after sanitization.
- `samples/HornetStudioDemo/Folders/main/Signals/Enhanced/.gitkeep` -> Placeholder for future EnhancedSignals YAML extraction.
- `samples/HornetStudioDemo/Folders/main/Signals/Custom/.gitkeep` -> Optional placeholder if useful for documenting the intended signal split.
- `samples/HornetStudioDemo/Folders/main/Scripts/Functions/` -> Include portable function YAML files if present or migrated from legacy workflows.
- `samples/HornetStudioDemo/Folders/main/Scripts/Workflows/` -> Keep only if needed to represent legacy compatibility.
- `samples/HornetStudioDemo/Folders/main/Applications/` -> Include only portable application definitions/scripts.
- `samples/HornetStudioDemo/Folders/main/Assets/` -> Include only portable assets.
- `samples/HornetStudioDemo/README.md` -> Usage and maintenance notes.
- `.gitignore` -> Add sample runtime-output ignores if needed, such as `samples/HornetStudioDemo/Logs/`.

## Implementation Order
1. Inspect `T:/HornetStudio/NewFirst/project.aaep` and `Folders/main/Folder.yaml` for absolute paths, passwords, secrets, and generated values.
2. Inspect `Applications`, `Assets`, and `Scripts` for portable files and skip generated or machine-local content.
3. Create `samples/HornetStudioDemo/` and copy only the sanitized portable subset.
4. Add the future resource directories `Signals/Enhanced` and optionally `Signals/Custom`.
5. Add `.gitkeep` files only for intentionally empty directories.
6. Add `samples/HornetStudioDemo/README.md`.
7. Update `.gitignore` if needed to prevent sample logs or runtime output from being tracked.
8. Validate that the sample project can be loaded manually or at least parsed by existing project/layout loading code.
9. Run the narrowest useful build or parser verification.

## Technical Constraints
- Chat output must stay German; file content, code comments, documentation text, and UI text must be English.
- Do not add the sample as a project to `HornetStudio.sln`.
- Do not commit runtime logs from `T:/HornetStudio/NewFirst/Logs`.
- Do not commit real secrets, tokens, passwords, or machine-specific absolute paths.
- Keep sample files portable across machines and checkouts.
- Prefer existing project file formats; do not invent a new demo manifest.
- Keep the demo project small enough to be practical for repeated QA.

## Acceptance Criteria
- `samples/HornetStudioDemo/project.aaep` exists and is portable.
- `samples/HornetStudioDemo/Folders/main/Folder.yaml` exists and can serve as a real demo folder layout.
- No files under `samples/HornetStudioDemo/Logs/` are tracked.
- No `T:/...`, `B:/...`, user profile paths, secrets, or unsanitized passwords are present in tracked sample files.
- `samples/HornetStudioDemo/Folders/main/Signals/Enhanced/` exists as the target location for future EnhancedSignals YAML files.
- The sample README explains how to open and maintain the demo.
- The demo is not included in the solution as a build project.

## Verification
- Search sample files for absolute local path markers:
  - `rg -n "T:|B:|C:\\\\Users|password|Password|token|secret" samples/HornetStudioDemo`
- Confirm no runtime logs are tracked:
  - `git status --short samples/HornetStudioDemo`
- Build or parse verification:
  - Prefer `dotnet build src/HornetStudio/HornetStudio.csproj --no-restore` for a focused app build.
  - If a lightweight loader test exists or can be reused without broad changes, verify the sample `project.aaep` and `Folder.yaml` parse successfully.
- Optional manual check:
  - Open `samples/HornetStudioDemo/project.aaep` in HornetStudio and confirm the main folder loads.

## Out of Scope
- Implementing `Signals/Enhanced/*.yaml` loading.
- Moving existing EnhancedSignals definitions out of `Folder.yaml`.
- Creating a generic sample project framework.
- Adding CI jobs.
- Adding generated logs, local app state, or binary runtime output.

## Risks / Watchouts
- The external reference may contain local runtime paths or generated state inside `Folder.yaml`; inspect before copying.
- `project.aaep` includes password fields in the reference. Sanitize them rather than preserving real credentials.
- Empty directories are not tracked by Git; use `.gitkeep` only for intentional future target directories.
- Copying `Logs/` would create noisy diffs and stale runtime state; exclude it.
- If Python application files are included, inspect them for local paths, credentials, environment assumptions, and generated virtual environment folders.

## Relevant Files
- `T:/HornetStudio/NewFirst/project.aaep`
- `T:/HornetStudio/NewFirst/Folders/main/Folder.yaml`
- `T:/HornetStudio/NewFirst/Folders/main/Applications/`
- `T:/HornetStudio/NewFirst/Folders/main/Assets/`
- `T:/HornetStudio/NewFirst/Folders/main/Scripts/`
- `samples/HornetStudioDemo/README.md`
- `samples/HornetStudioDemo/project.aaep`
- `samples/HornetStudioDemo/Folders/main/Folder.yaml`
- `.gitignore`

## Notes
- This demo project is a repository sample and QA fixture, not a compiled project.
- The planned EnhancedSignals direction should use `Signals/Enhanced/*.yaml`, because Enhanced Signals are signal resources, not scripts.
- Keep the sample as the shared baseline for later Functions, EnhancedSignals, CustomSignals, and Client browser work.
